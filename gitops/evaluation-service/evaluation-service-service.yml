apiVersion: v1
kind: Service
metadata:
  name: evaluation-service-svc
  namespace: fiap-tc-f4
 
spec:
  selector:
    app: evaluation-service
  ports:
  - port: 8004
    targetPort: 8004
    protocol: TCP