apiVersion: v1
kind: Service
metadata:
  name: analytics-service-svc
  namespace: fiap-tc-f4
spec:
  selector:
    app: analytics-service
  ports:
  - port: 8005
    targetPort: 8005
    protocol: TCP